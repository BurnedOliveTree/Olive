add(first: Int, second: Int): Int {
    return first + second;
}

multiply(first: Int, second: Int): Int {
    return first * second;
}

chainCall(number: Int): Int {
    return add(number, 2).multiply(7).add(-1);
}

main(): Unit {
    var number: Int = 6;
    add(number, 2).multiply(7).add(-1);
}